from django.contrib import admin
from django.db import models
from sim.models import * 

# exclude these because they are functionally abstract
#admin.site.register(CardTemplate)
#admin.site.register(Card)

